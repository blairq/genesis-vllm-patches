[pytest]
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    cuda_required: test requires NVIDIA CUDA device
    rocm_required: test requires AMD ROCm device
    gpu_required: test requires any GPU (CUDA or ROCm)
    slow: test takes >5 seconds
    input_size_per_partition: internal vLLM Linear attribute (suppressed warning)
    output_size_per_partition: internal vLLM Linear attribute (suppressed warning)
addopts = -v --tb=short --strict-markers
